#include<bits/stdc++.h>
using namespace std;

typedef long long ll;

int main(){
    int n, k;
    cin >> n >> k;
    vector<int> arr(n);
    for(int i=0; i<n; i++) cin >> arr[i];
    map<int,set<int>> mpp;
    map<int,int> freq;
    int i = 0;
    int j = 0;
    while(j < n){
        int f = freq[arr[j]];
    }
    return 0;
}